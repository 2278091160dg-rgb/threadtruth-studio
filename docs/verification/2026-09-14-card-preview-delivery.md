# Fixed-card preview delivery — 2026-09-14

Scope: approved development-layer fixed-card plan, local integration and the first real Korean C layout. This is not a release, maintainer visual signoff, new host lifecycle test or completed 24-style gallery.

## Delivered

- Schema4 `compose` retains native output, exact prompt/call receipt and observed extraction rectangles; records fixed-card transforms, local font identity/hash, labels, native/display/thumbnail hashes and pending human review. Schemas1–3 remain read-only history.
- 1200×1200 board; six360×480 cards at x44/420/796 and y140/636. Each entire observed pose panel is contained without upscale or stretch, using padding where needed. Exact3:4 describes the cards, not an asserted native geometry pass. Local title and footer bands explicitly identify the derivative.
- Rights, style-page and README projections distinguish all three media roles; unapproved collections cannot be promoted. Synthetic24-sheet tests cover all72assets and projection rollback; these fixtures never count as real pictures or approvals.
- Primary validation and its JSON schema accept B1/B and C1/C final workflows. Mode/action mismatches, non2:3 canvas, test/preview contamination and anything other than six distinct finals remain rejected.
- Current bilingual documents and the21-package schedule are synchronized; additional independent-final representatives are optional. No runtime file changed.

## Real pilot, still private and awaiting review

The registered schema3 Korean C original and actual generation receipt were reused in a fresh run. The original and prompt are byte-identical; the generation record is unchanged. No native generation, retry or image repair occurred in this batch. All previous attempts and their native-geometry findings remain unchanged, including the third independent trial.

| Asset | Dimensions | Bytes | SHA-256 |
|---|---|---|---|
| Retained native original | 1254×1254 | retained privately | `4916805f167e5945acf1d25a4f8d1dbd5534dc93c524bfbf62e39724245e75bb` |
| Native whole-sheet JPEG | 1254×1254 | 246280 | `263c910f399c8cf79bec8db4dd57296271ba930282d756c1fa30a15867539882` |
| Composed display JPEG | 1200×1200 | 241561 | `42cedcbad017560bc0d024c52cfb3ec6945c6776c760f6f057f5d1fd354644b8` |
| Whole-board thumbnail | 600×600 | 80887 | `61017eeeed5da76829f8d38982a8e7833abd677fd507aa663a84a8d2f711d536` |

Observed source rectangles (x,y,width,height), row-major: `(0,104,414,518)`, `(419,104,414,518)`, `(839,104,415,518)`, `(0,627,414,560)`, `(419,627,414,560)`, `(839,627,415,560)`. Source panels are not3:4. The layout preserves their proportions within3:4 cards; it does not invent missing content. The controller viewed both original and derived images; final product/identity/pose/style acceptance belongs to DENGGUI.

Style-scoped machine audit: PASS. Full collection audit: correctly BLOCKED by23missing native outputs and the Korean sheet's incomplete human review. Machine-layout coverage1/24; human-approved0/24; public0/24; independent-final style coverage1/24 and complete primary cases1/3. Historical native attempts remain3; new calls0, retries0. No review template was filled on behalf of the maintainer.

## Verification results

| Check | Actual result |
|---|---|
| Full repository unittest suite | Final120/120 PASS,83.627s, no skipped tests |
| Focused core preview tests |31PASS before integration; real scoped audit also PASS |
| Primary and release checks |17primary and6release tests PASS; included in final full run |
| 24pack strict lint |24/24 PASS; registered longest-match neighbor notes retained |
| Trigger regression |132ownership,22query,3score-ledger,47style-target,13style-conflict and1core-conflict assertions PASS; not a real host discovery test |
| System Skill / governor quick |PASS |
| Source strict / isolated runtime strict |PASS; source changelog section structure corrected before rerun |
| Official Plugin validator |PASS on isolated allowlist stage |
| Public tree and full local Git history scan |PASS; ignored originals/review workspace excluded, force-tracked sensitive fixtures still rejected |
| Structured parsing |39JSON,32YAML,28Python files parsed; actual schema4 pilot and B1/C1 rights schema checks PASS |
| Runtime preservation |35/35 files byte-identical to pre-batch source |
| Current homepage/guide/style local links |36existing file links resolved; future24thumbnail rendering covered with local fixtures |
| Browser checks |Actual gallery and offline guide at1440px and390px: no horizontal overflow, loaded images with alt text, keyboard links usable, zero external requests and zero console errors in offline mode; guide print navigation hidden |
| Local build |Allowlist staging PASS; private candidates, raw originals, receipts, fonts, tools/tests and review drafts excluded |
| Existing beta.2 download entries |Read-only GitHub API confirms the published tag, Plugin ZIP and matching checksum asset URLs; no asset uploaded or replaced |

The browser used an isolated headless context, not a logged-in browser profile. The sandbox initially blocked browser startup; permissioned isolated execution then passed. CI now installs and checks a development-only CJK font before tests; the remote Linux workflow has not been run in this batch. No font file is redistributed and no runtime dependency was added.

The local validation archive retains the current manifest version only for build checks; it is NOT a replacement beta.2 release. It must not overwrite published beta.1/beta.2 assets. A separately authorized beta.3 remains the next publication target.

## Findings closure

| Finding | Closure |
|---|---|
| Legacy preview evidence raised an unaggregated rights-index error |fixed: narrow invalid-record handling; existing release rejection test changed from RED to GREEN |
| Legacy/null style-index preview projection raised KeyError/TypeError |fixed: failing regression for both shapes, minimal validator handling, independent re-review PASS |
| C1 producer support disagreed with B1-only rights schema |fixed: failing producer/schema parity assertion, schema enum extended toB1/C1, actual JSON-schema validation PASS |
| Missing CJK font could skip rendering tests on CI |fixed in workflow: required package and exact-file check; actual Linux run remains unperformed |
| Task1 fixed-card core review |PASS; no blocking findings |
| Task2 consumer integration review |PASS after legacy-projection fix; no unresolved findings in reviewed scope |
| Final delivery consistency and image comparison |PASS: actual hashes, coverage counts and current bilingual docs match; no obvious extraction loss or text overlap observed by the independent assistant reviewer. This is not maintainer QA |
| Issue1 minimal implicit invocation |open / needs-reproduction: retained summary lacks exact prompt, first answer and load trace; cannot attribute a repository defect or host bug. Explicit invocation guidance remains; paired recognition-only checks belong to external lifecycle testing |

## Not completed or authorized here

Remaining23style generations and each approval;24/24visual acceptance and public gallery; GitHub push or beta.3 Release; new garment rights and two complete cases; external lifecycle, recording and five installations; external-feedback repair release;30elapsed Beta days and stable release; final application authorization/submission. These are tracked in [WORK-STATUS](../WORK-STATUS.md), not replaced by static tests or downloads. No external state or global Skill installation was changed.
